rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Open rules for local development and testing of all 4 collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
